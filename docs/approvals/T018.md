# T018 승인 마커

```yaml
approved_by: "이훈"
approved_at: "2026-07-06T23:59:00+09:00"
retroactive: false
scope_confirmation: "TTS 실경로 연결(T018) — OpenAI gpt-4o-mini-tts + coral (ADR-0001 확정안) 어댑터 실구현, presynth+cache_layer 연결, tts_generate_start/complete·cache_hit/miss 계측. 실과금 한도: 실측 신규합성 ≤ 50회, 예상 비용 ≤ $1 (실측 $0.0078/miss 기준 ≈ $0.39 + 버퍼). 키는 OPENAI_API_KEY 환경변수 주입만 — 코드·로그·픽스처에 키/응답 원문 금지 (runbook §4). 키 부재 환경에서는 계약 테스트 skip + mock GREEN 유지."
rollback_plan: "git revert <commit> — 어댑터는 mock 폴백 유지라 revert 시 T016 상태로 복귀. 과금은 실행 시점에만 발생(코드 잔존 리스크 없음)."
```

- 승인 근거: 2026-07-06 채팅에서 운영자가 T018 등록·승인 마커 생성을 명시 지시 (과금 한도 옵션 포함 선택).
- 관련: docs/tickets/T018-tts-real-provider-presynth.md, docs/decisions/0001-tts-voice-and-provider.md, docs/master-spec.md §1.4·§1.6·§3
