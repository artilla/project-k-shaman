# T028 preflight evidence — 2026-07-15

This record covers reversible repository work and a non-executed AWS change set.
It is not a staging deployment record and does not satisfy the HTTPS/OAuth or
rollback-drill acceptance criteria.

## Repository and local runtime

- approval marker base revision: `7fadb4d`
- `./ralph/scripts/run_checks.sh --full`: pass
  - pytest: 301 passed
  - Bats: 430 passed (DB cases skipped by the existing no-`initdb` guard)
  - frontend typecheck/build and deployment contract: pass
- `docker build --tag shindang:test .`: pass with digest-pinned base images and
  hash-locked Python runtime dependencies
- isolated Compose smoke: pass
  - SPA, `/healthz`, `/readyz`, `/api/auth/providers` served through Caddy
  - app ran as `10001:10001` with a read-only root filesystem
  - app port had no published host port; only the Caddy test ports were published
- GitHub workflow syntax (`actionlint v1.7.7`): pass
- CloudFormation `validate-template`: pass

## AWS preflight

- region: `ap-northeast-2`
- stack: `shindang-staging`
- stack state: `REVIEW_IN_PROGRESS`
- change set: `awscli-cloudformation-package-deploy-1784078378`
- change-set state: `CREATE_COMPLETE`, `AVAILABLE`, not executed
- planned resources: ECR repository, deployment bucket, app secret, log group,
  security group, least-privilege instance role/profile, GitHub deploy role,
  ARM64 EC2 instance, and EIP
- billable resources created by this preflight: none

## Post-review change-set gate

- The P1 hardening patch adds `s3:GetObjectVersion` to the instance role and
  therefore changes the reviewed CloudFormation template.
- The named `AVAILABLE` change set above predates that patch. It remains
  unexecuted, but it is superseded and **must not be executed**.
- After the external hostname/OAuth/OIDC inputs are confirmed, create a new
  change set from the exact merged revision, compare it with this record, and
  only then request apply approval. This repository session does not recreate
  or execute the AWS change set.

## Post-review repository validation

- `./ralph/scripts/run_checks.sh --full`: pass after the P1 hardening patch
  - pytest: 301 passed
  - Bats: 434 passed (DB cases skipped by the existing no-`initdb` guard)
  - frontend typecheck/build and deployment contract: pass
- registry-prefix/region, required VersionId, and deployment-environment regression
  tests: 4 passed
- GitHub workflow syntax (`actionlint v1.7.7`): pass
- CloudFormation lint (`cfn-lint 1.39.1`): pass
- deployment shell lint (`ShellCheck 0.10.0`): pass

## GitHub Action pin verification

Verified on 2026-07-15 against each publisher's GitHub release and the peeled
tag object returned by `git ls-remote --tags`. All 14 workflow references map to
the following eight unique, exact tag commits:

| Action | Release tag | Pinned commit | Result |
| --- | --- | --- | --- |
| `actions/checkout` | `v4.3.1` | `34e114876b0b11c390a56381ad16ebd13914f8d5` | exact |
| `actions/setup-python` | `v5.6.0` | `a26af69be951a213d495a4c3e4e4022e16d87065` | exact |
| `actions/setup-node` | `v4.4.0` | `49933ea5288caeca8642d1e84afbd3f7d6820020` | exact |
| `aws-actions/configure-aws-credentials` | `v4.3.1` | `7474bc4690e29a8392af63c5b98e7449536d5c3a` | exact |
| `docker/setup-qemu-action` | `v3.7.0` | `c7c53464625b32c7a7e944ae62b3e17d2b600130` | exact |
| `docker/setup-buildx-action` | `v3.12.0` | `8d2750c68a42422c14e847fe6c8ac0403b4cbd6f` | exact |
| `aws-actions/amazon-ecr-login` | `v2.1.6` | `d539f0932e70871a027e9d5a9d8fc38589180a64` | exact |
| `actions/upload-artifact` | `v4.6.2` | `ea165f8d65b6e75b540449e92b4886f43607fa02` | exact |

No mutable action tag is executed by these workflows; the tag names remain
comments for reviewer readability only.

## Remaining external inputs and gates

- Confirm a staging hostname and the DNS operator that can point it at the EIP.
  This AWS account has no Route 53 hosted zone, so the change set is intentionally
  left unexecuted to avoid idle EC2/EIP charges.
- Confirm that the account's GitHub Actions OIDC provider exists or provide an IAM
  operator able to verify it. The current principal cannot inspect OIDC providers.
- Create or identify Google and Kakao staging OAuth applications and authorize the
  exact HTTPS callback origins. No OAuth credential value is present in the repo.
- Configure GitHub `staging` environment approval and non-secret variables after
  the stack is applied, including `SHINDANG_ENV=staging`. Configure the
  production environment independently with `SHINDANG_ENV=production` before
  any promotion.

## Scope assertion

- migrations 003-005 executed: no
- DB role/schema/data write executed: no
- production resource changed: no
- secret value, account identifier, DB host, or personal test identity recorded: no
